#!/usr/bin/env bash
# enable_exam_mode.sh (a safe wrapper — review before running)
echo "This script sets up auto-login for examuser and enables kiosk & watchdog services."
echo "Review the script in the repo before running on a real machine."
